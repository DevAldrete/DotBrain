# Frontend environment variables

# API key for authenticating requests to the DotBrain backend.
# Must match the API_KEY set in the backend .env file.
# Leave empty if the backend is running without auth (local dev).
VITE_API_KEY=
